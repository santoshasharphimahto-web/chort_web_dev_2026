services:
  postgres:
    image: postgres:15.3
    container_name: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: oidc_connect
    ports:
      - "5432:5432"
    volumes:
      - ./data/postgres:/var/lib/postgresql/data